import CONFIG_SHARED from '../../../shared/config/CONFIG_SHARED';

const CONFIG = {
	...CONFIG_SHARED,
	RENDER: {
		PASS: {
			THRESHOLD: 0,
			STRENGTH: 0,
			RADIUS: 0,
			EXPOSURE: 1,
		},
	},
};

export default CONFIG;
